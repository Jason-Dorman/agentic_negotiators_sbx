/**
 * Shared protocol definitions: schemas, ABI artefacts, fixtures and reason tables.
 *
 * Contents arrive in stage 1 of docs/build_plan.md. The normative source is
 * docs/protocol.md; this package is its machine-readable form and nothing more.
 */

/** docs/protocol.md section 15. Frozen at release v0.1. */
export const PROTOCOL_VERSION = 1 as const;
